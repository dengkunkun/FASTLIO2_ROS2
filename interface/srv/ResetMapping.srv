# ResetMapping.srv
# 重置建图服务 - 支持在已有地图上重新建图
#
# @brief 重置 FASTLIO2 建图状态，可选加载已有地图作为初始地图
# @author firebot
# @date 2026-01-04
#
# 使用场景:
# 1. 固定点重建: 直接指定初始位姿，从零开始建图
# 2. 已有地图继续建图: 加载 PCD 作为初始地图，从指定位姿继续增量建图
# 3. 重定位后重建: 先调用 /relocalize 获取位姿，再调用本服务重置

# --- 请求 ---
# 初始位姿 (在 map_frame 坐标系下)
float64 x
float64 y
float64 z
float64 roll    # 绕 X 轴旋转 (rad)
float64 pitch   # 绕 Y 轴旋转 (rad)
float64 yaw     # 绕 Z 轴旋转 (rad)

# 地图加载选项
bool load_map           # 是否加载已有地图
string map_pcd_path     # PCD 地图文件路径 (仅当 load_map=true 时有效)

# IMU 偏置选项
bool reuse_bias         # 是否复用上一次估计的 ba/bg，减少重新收敛时间

# 状态清除选项
bool clear_path         # 是否清除已有轨迹记录

---
# --- 响应 ---
bool success
string message
